# Python File Sorter

A simple command-line tool that organizes files in a selected folder by file type.

## Run

```bash
python file_sorter.py
```

Then paste the folder path.

## Features

- Detects common file extensions.
- Creates category folders automatically.
- Avoids overwriting files with the same name.
- Keeps the script small and easy to improve.
